<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teratotech.myusrah.myusrah" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.teratotech.myusrah.myusrah.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Subscription activity -->
        <activity
            android:name="com.teratotech.myusrah.myusrah.Subscription"
            android:label="@string/title_activity_subscription"
            android:parentActivityName="com.teratotech.myusrah.myusrah.MainActivity" >
        </activity>


    </application>

</manifest>
